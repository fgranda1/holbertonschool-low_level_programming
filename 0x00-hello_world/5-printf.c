#include <stdio.h>
#include <string.h>
/**
 * main - main block
 * Return: 0
 */
int main(void)
{

	char msg[55];

	strcpy(msg, "with proper grammar, but the outcome is a piece of art,");

	puts(msg);
	return (0);
}
