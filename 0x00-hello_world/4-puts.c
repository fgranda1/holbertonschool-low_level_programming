#include <stdio.h>
#include <string.h>
/**
 * main - main block
 * Return: 0
 */
int main(void)
{

	char msg[55];

	strcpy(msg, "\"Programming is like building a multilingual puzzle");

	puts(msg);
	return (0);
}
